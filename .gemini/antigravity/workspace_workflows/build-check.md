# Build Check

Chạy `pnpm build` trong project và phân tích kết quả:

1. Nếu build **thành công** → báo "✅ Build passed" kèm summary (thời gian, số trang)
2. Nếu build **thất bại** → liệt kê từng lỗi, chỉ rõ file + dòng, và đề xuất cách fix

Không tự động sửa code. Chỉ báo cáo và đề xuất.
